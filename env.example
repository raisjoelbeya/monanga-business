# URL de l'application (sans barre oblique à la fin)
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_DOMAIN="localhost"
NEXTAUTH_SECRET="your-secret-key-here" # Générez une clé sécurisée avec: openssl rand -base64 32

# Configuration de la base de données
DATABASE_URL="postgresql://user:password@localhost:5432/monanga_business?schema=public"
## Pour Prisma Migrate (connexion non poolée, conseillé sur Neon)
# Utilisez la connexion directe (primary) pour les migrations, et laissez DATABASE_URL pointer vers le pooler
# Exemple Neon (remplacez <...>):
# DIRECT_URL="postgresql://<user>:<password>@<primary-host>/<db>?sslmode=require"
# Exemple local (souvent identique à DATABASE_URL):
# DIRECT_URL="postgresql://user:password@localhost:5432/monanga_business?schema=public"

# Google OAuth Configuration
GOOGLE_CLIENT_ID="votre-google-client-id"
GOOGLE_CLIENT_SECRET="votre-google-client-secret"

# Facebook OAuth Configuration
FACEBOOK_CLIENT_ID="votre-facebook-app-id"
FACEBOOK_CLIENT_SECRET="votre-facebook-app-secret"

# Configuration de l'email (pour les notifications et réinitialisation de mot de passe)
EMAIL_SERVER_HOST="smtp.example.com"
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER="votre-email@example.com"
EMAIL_SERVER_PASSWORD="votre-mot-de-passe-email"
EMAIL_FROM="Monanga Business <no-reply@example.com>"

# URL de redirection après connexion/déconnexion
NEXTAUTH_URL_INTERNAL="http://localhost:3000"

# Configuration pour les cookies de session (optionnel)
NEXTAUTH_COOKIE_SECURE="false" # Mettre à 'true' en production avec HTTPS
NEXTAUTH_COOKIE_HTTP_ONLY="true"
NEXTAUTH_COOKIE_PATH="/"
NEXTAUTH_COOKIE_SAME_SITE="lax"
NEXTAUTH_COOKIE_NAME="__Secure-next-auth.session-token" # Pour HTTPS

# Configuration CORS (si nécessaire)
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# Configuration pour le développement
NODE_ENV="development"

# Configuration pour les logs
DEBUG="false"

# Configuration du taux de limitation (rate limiting)
RATE_LIMIT_WINDOW_MS=900000 # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100 # Nombre maximum de requêtes par fenêtre
